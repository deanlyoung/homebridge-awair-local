# Contributing

Issues and pull requests are welcome. Please use the current supported Node.js LTS versions (22 and 24), run `npm run lint` and `npm test`, and keep pull requests focused.

Changes that alter configuration must update `config.schema.json`, `config-sample.json`, and the README. Releases are published from `master`; each published version receives a Git tag and GitHub release notes through the release workflow.
